# Personal Website

This repository contains the static files for my professional website. The homepage presents a curated research profile, selected publications, and current open-source software.

`publications.html` is a separate Google Scholar snapshot generated by `update_publications.py`. It is not used to render the curated publication list on the homepage. The accompanying GitHub Action is manual-only and requires the `SERPAPI_API_KEY` repository secret.
